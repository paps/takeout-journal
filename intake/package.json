{
	"name": "takeout-journal-intake",
	"private": true,
	"version": "0.0.1",
	"description": "",
	"scripts": {
		"lint": "echo 'tslint...' && tslint 'src/**/*.ts'",
		"build": "npm run lint && echo 'tsc...' && tsc",
		"clean": "rm -rf dist || true",
		"re": "npm run clean && npm run build"
	},
	"repository": {
		"type": "git",
		"url": "git+https://github.com/paps/takeout-journal.git"
	},
	"author": "Martin Tapia <contact@martintapia.com> (https://martintapia.com)",
	"license": "ISC",
	"bugs": {
		"url": "https://github.com/paps/takeout-journal/issues"
	},
	"devDependencies": {
		"@types/moment": "^2.13.0",
		"@types/node": "^10.12.18",
		"@types/papaparse": "^4.5.6",
		"tslint": "^5.12.0",
		"typescript": "^3.2.2"
	},
	"dependencies": {
		"moment": "^2.23.0",
		"papaparse": "^4.6.2"
	}
}
